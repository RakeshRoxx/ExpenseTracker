package com.amaterasu.expense_tracker.worker

import android.Manifest
import android.os.Build
import androidx.activity.result.contract.ActivityResultContracts

object ForegroundPermission {
//    private val requestFgsDataSyncPermission =
//        registerForActivityResult(ActivityResultContracts.RequestPermission()) { granted ->
//            if (!granted) {
//                // User denied; you can disable background sync gracefully
//            }
//        }

//    private fun ensureFgsPermissions() {
//        if (Build.VERSION.SDK_INT >= 34) {
//            // Request granular FGS permission for data sync
//            requestFgsDataSyncPermission.launch(
//                Manifest.permission.FOREGROUND_SERVICE_DATA_SYNC
//            )
//        }
//    }
}